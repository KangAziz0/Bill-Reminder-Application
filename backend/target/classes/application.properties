# Server
server.port=8081

# Database (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/bill_reminder
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.main.allow-circular-references=true

# JWT
app.jwt.secret=billreminderSecretKey2024billreminderSecretKey2024billreminderSecretKey2024
app.jwt.expiration-ms=86400000

# Mail (configure with your SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=abdulazizgustian@gmail.com
spring.mail.password=
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# App
app.frontend.url=http://localhost:5173

# Scheduler timezone
spring.task.scheduling.pool.size=5

# Fonnte WhatsApp API
fonnte.api.url=https://api.fonnte.com/send
fonnte.api.token=cc5fcQRuLb9TvvmhoS1r
